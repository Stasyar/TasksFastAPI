### FastAPI App

#### .env
```text
MODE = DEV
POSTGRES_DB = dev_db
POSTGRES_USER = postgres
POSTGRES_PASSWORD = postgres
DB_HOST = localhost
DB_PORT = 5432
```

### start
```bash
docker compose up --build
```